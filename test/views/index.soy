{namespace closuretest}

/**
 * Test template
 */
{template .index}
Hello world!
{/template}

/**
 * List test template
 * 
 * @param name Name of the list
 * @param list A list
 */
{template .list}
{$name}:{sp}
{foreach $item in $list}
	{$item}{if not isLast($item)},{sp}{/if}
{/foreach}
{/template}
